#include <stdio.h>
/**
 * main - Prints a text
 *
 * Return: Always 0 (Successful)
 */
int main(void)
{
        printf("\"with propper grammar, but the outcome is a peice of art,\n");
        return (0);
}

